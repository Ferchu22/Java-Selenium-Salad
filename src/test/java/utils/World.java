package utils;

public class World {  // this class is used as a temporary container between steps

    public int cucumbers = 0; //Tracks the number of cucumbers (initialized to 0).
    public int carrots = 0;   //Tracks the number of carrots (initialized to 0).
    public int salads = 0;   //Tracks the number of salads (initialized to 0).
    public String response= "";
    public boolean isError = false;

    // public String errorMessage = "You cant eat more carrots than you have";  // this one  is to store error message
}